import Ember from 'ember';

export default Ember.Controller.extend({
  actions: {
    login: function () {
      //TODO create the login function to make rest call, perhaps encrypt password
    }
  }
});
